describe("Test getEntityWorkerNeeded", () => {
  it("Context 1", () => {
    const a = 1
    expect(a).toEqual(1)
  })

  it("Context 2", () => {
    const a = null
    expect(a).toBeNull()
  })
})
